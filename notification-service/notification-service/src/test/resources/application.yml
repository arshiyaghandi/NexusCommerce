eureka:
  client:
    enabled: false

spring:
  profiles:
    active: test
  cloud:
    discovery:
      enabled: false
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.kafka.KafkaAutoConfiguration
